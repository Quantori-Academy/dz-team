{
    "name": "be",
    "version": "0.0.0",
    "scripts": {
        "build": "tsc",
        "dev": "cross-env NODE_ENV=development ts-node src/index.ts",
        "preview": "yarn build && cross-env NODE_ENV=development node dist/index.js",
        "prod": "yarn build && cross-env NODE_ENV=production yarn preview"
    },
    "dependencies": {
        "@fastify/cors": "^9.0.1",
        "fastify": "^4.28.1"
    },
    "devDependencies": {
        "@types/node": "^22.5.2",
        "cross-env": "^7.0.3",
        "ts-node": "^10.9.2",
        "typescript": "^5.5.4"
    }
}
