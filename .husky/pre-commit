current_branch=$(git symbolic-ref HEAD | sed -e 's,.*/\(.*\),\1,')

if [ "$current_branch" = "main" ]; then
  echo 'You are committing to "main" branch. This is available for local development, but pushes to "main" are not allowed. For prod changes please create a new branch and submit a pull request.'
fi

lint-staged
